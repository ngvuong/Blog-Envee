import styled from 'styled-components';

function Editor() {
  return (
    <StyledContainer>
      <h1>Blog Editor</h1>
    </StyledContainer>
  );
}

const StyledContainer = styled.main`
  padding: 5rem;
`;

export default Editor;
